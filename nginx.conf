server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Match GitHub Pages' clean-URL behavior: /privacy -> privacy.html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
